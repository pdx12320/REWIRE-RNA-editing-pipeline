# Scripts

Run shell files with `bash` and Python files with `python3`. The REDItools2 runner processes all six samples, creates coverage maps, merges calls, adds headers, and creates tabix indexes. The Python helpers build a union VCF, interpret transcript-strand C-to-U calls, and compare treated and control replicates.

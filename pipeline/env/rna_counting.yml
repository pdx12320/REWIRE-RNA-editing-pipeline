name: rewire_rna_counting
channels:
  - conda-forge
  - bioconda
dependencies:
  # Supported repository environment, not the frozen production interpreter.
  - python=3.12
  - pysam=0.24.*
  - samtools=1.23.*

